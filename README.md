Focus Planner – AI Task Manager + MAX Messenger Bot

Этот проект включает:

AI-ассистента для составления расписания

MiniApp интерфейс (HTML/CSS/JS)

Backend на FastAPI

Чат-бота для MAX Messenger

Docker-контейнер для backend

1. Требования

Python 3.11+

Docker Desktop (необязательно)

MAX-botapi-python (устанавливается автоматически)

Git (рекомендуется)

2. Установка зависимостей

В корне проекта выполните:

pip install -r requirements.txt


Это установит зависимости для:

backend

MiniApp (нет зависимостей)

чат-бота MAX

Все нужные токены хранятся в tokens.rar, который выслан отдельно

3. Запуск backend (2 варианта)
Вариант A — запуск через Docker (рекомендовано модерации)

Перейти в директорию backend:

cd backend


Построить образ:

docker build -t focus-planner .


Запустить контейнер:

docker run -d -p 8000:8000 --name focus-planner-container \
  -e HF_TOKEN="ВАШ_ТОКЕН" \
  focus-planner


Проверить:

http://127.0.0.1:8000/tasks

Вариант B — запуск без Docker
cd backend
uvicorn app:app --reload --host 0.0.0.0 --port 8000

4. Запуск MiniApp (frontend)

Перейдите в:

cd frontend


Запустите встроенный сервер:

python -m http.server 5500


MiniApp будет доступен по адресу:

http://127.0.0.1:5500


Вкладка AI, Today, Goals и Pomodoro будут работать только если backend запущен.

5. Запуск чат-бота MAX

Файл:

/bot/chat-bot.py


Перед запуском вставьте свой токен MAX Messenger:

bot = Bot("ВАШ_ТОКЕН")


Запуск:

cd bot
python chat-bot.py


Бот сразу начинает принимать сообщения.

6. Структура проекта
focus-planner/
│
├── backend/
│   ├── app.py
│   ├── db.py
│   ├── data.db
│   ├── Dockerfile
│
├── frontend/
│   ├── index.html
│   ├── style.css
│   └── script.js
│
├── bot/
│   └── chat-bot.py
│
├── requirements.txt
├── README.md
└── Трек_.pdf

📝 7. Контакты и поддержка

Почта: ivan.kazancev.06@bk.ru ; karaskarasevic1@gmail.com  
Номер телефона: +79178174420 ; +7 996 729 9510
Проект создан для хакатона.